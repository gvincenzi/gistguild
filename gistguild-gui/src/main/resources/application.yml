info:
  name: Gist Guild - GUI
  artifact: '@project.artifactId@'
  version: '@project.version@'

spring:
  application:
    name: gistguild-gui
  rabbitmq:
    addresses: amqp://test
  cloud:
    stream:
      bindings:
        distributionChannel:
          destination: distributionChannel

gistguild:
  bot:
    username: gist_it_bot
    token: TOKEN
    entryFreeCredit:
      amount: 5
    stripe:
      active: false
      token: 0000000:TEST:0000000000000000

  spike:
    username: username
    password: password
    url: http://localhost:8882/spike

